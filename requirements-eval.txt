# Pinned Python dependencies for the eval spike harness (eval/, sim/spike/) -- devops-owned.
#
# The avoidance/coverage/geofence core is intentionally stdlib-only, but ten test modules in
# tests/fieldguard_planning/ import numpy, so a bare interpreter exits 1 there with 614 of 822 green
# (unittest's loader turns a module-scope ImportError into an ERROR, not a skip) -- install this file
# first. Only `python3 -m unittest discover -s tests -p 'test_*.py'` (57, OK) is genuinely
# install-free; CI installs this file BEFORE running any test root, deliberately.
# Python floor: these pins need **Python 3.11+** (CI pins 3.12). On the 3.9 macOS host, pip reports
# "No matching distribution found" for numpy==2.5.1 -- use a 3.11+ interpreter or a venv.
# Weeks 5-6 added ONE scoped exception to the stdlib-only rule: the NDVI fusion + georef-stitch tests
# (test_ndvi_fusion.py, test_ndvi_georef.py, testing src/fieldguard_planning/ndvi_fusion.py /
# ndvi_georef.py) need numpy for pixel-array math -- documented as a deliberate, scoped deviation in
# ndvi_fusion.py's own module docstring, not a project-wide dependency change. They reuse THIS same
# numpy pin rather than introducing a second one.
#
# Install:  pip install -r requirements-eval.txt
# Verified: Python 3.12.12, macOS arm64 (2026-08-04) and targeted at the CI runner's
#           actions/setup-python@v5 python-version "3.12" on ubuntu-latest.
#
# Consumers: sim/spike/gen_spike_clip.py (numpy), eval/spike_common.py, eval/label_from_sim.py,
# eval/baseline_ndvi.py, eval/baseline_rgb.py, src/fieldguard_planning/ndvi_detect.py
# (scipy.ndimage -- the ADOPTED ADR-003 detector; it moved out of the deleted eval/blob.py so the
# flight path and this harness share one implementation, and the sim image installs python3-scipy
# for the same reason), eval/score.py (stdlib only, no direct third-party import, but shares the
# same venv for `eval/run_spike.sh`), tests/fieldguard_planning/test_ndvi_fusion.py +
# test_ndvi_georef.py (numpy only, no scipy) + test_ndvi_detect.py (numpy AND scipy).
numpy==2.5.1
scipy==1.18.0
# Docs-site generator (scripts/build_docs_site.py, ADR-014) -- the second scoped exception, same
# policy as numpy above: one pinned dep, one consumer, declared here rather than a second
# requirements file. CI's docs-site job installs from THIS file so the pin has one home.
# Verified: markdown 3.9 on macOS (Python 3.9, 2026-08-18) and targeted at CI's python 3.12.
markdown==3.9

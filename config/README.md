# Config

Data-driven scenario + mission configuration so scenarios can be added without code changes:
- field polygon / boundary survey
- mission / coverage parameters
- bird trajectories (start with 2-3; scale later)
- sensor configs: single-NDVI (primary) and second-sensor comparison arm

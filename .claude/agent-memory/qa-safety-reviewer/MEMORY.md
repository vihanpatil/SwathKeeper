# qa-safety-reviewer memory

- [Safety scenario catalog](reference_safety_scenario_catalog.md) — scenarios, regressions, the two gate paths (they disagree: legacy CPA is vertex-only), truth track, suite baselines, mutation harness, offline-study review, ADR-019 depth probes, the synthetic-frame render-gate probe, the two-round (shape vs enforcement) mutation rule, the depth-segmenter layer's four independent re-derivations + the depth-WIRING layer's four (HEAD-diff of the default artifact, off-centre orientation, the 85-frame mission-realism run, the enum-reachability grep), and the P1 depth-log SCORING layer (independent synthetic log builder, one truth track per dir, the neighbour-gate confound)
- [Open safety gaps](project_open_safety_gaps.md) — to-break list by consequence; G43-G55 breaching take, G56-G60 + G74 CLOSED, G61-G77 replay/study, G78-G89 the ADR-019 depth sensor, G90-G100 the D2/D3 in-render harness rewrite, G101-G106 the D3->D4 acquisition handover (46.0 vs the printed 58.0 floor), G107-G114 the probe-C/G105 fix round (third corner copy, band_covered false BOOKABLE, D3 never reads depth), G115-G122 the commissioning-close doc lens (a rule no ADR contains, a sweep that misstates its own exit code, an artefact with no caveats), G125-G134 the harness/gate-integrity lens over the same tree (the unpinned `acq_range_m` conjunct, a 13.05 pin that clears by 0.15 mm, an uncapped escape, a booked speed nothing enforces), G135-G147 the booking-enforcement round (the injected param name does not exist at the pinned firmware SHA; the gated median cannot see an encounter flown at 2x the booking; 11/11 mutants killed), G148-G156 the depth segmenter (an FNR bar that passes by 1 pixel; the merge metric calls a correct split a merge; the NaN guard is untested), G157-G166 the depth-source WIRING (the booking gate calls a depth take "not an avoidance take"; a depth take can fly blind with all three nets absent; intrinsics/frame-size never compared), G167-G170 the dodge-take PRE-REGISTRATION (clutter-backed 28 m vs a 33.591 m breakeven; §0b forbids the flight), G171-G175 the P1 depth-log SCORING gates (G168 CLOSED; the AUTHORISING bar credits a 500 m range from a 60 m sensor; the runtime bars skip on an impossible counter; six docs still say UNSCOREABLE), G176-G188 the 2026-09-10 PORTFOLIO-FLOOR round (the NDVI seam has no frame-shape guard the depth seam makes HARD; a runbook that governs the next flight still says it has never been flown; a booking flag the launcher no-ops; five handoffs that never landed; G175 CLOSED), G189-G194 the 2026-09-11 OUTREACH/VIDEO round (gates credited with catches they were GREEN for; a hypothetical booking asserted as fact; 'sell the product' LIVE on the published docs site — G191 OPEN)
- [Docs evidence chain](reference_docs_evidence_chain.md) — which artifact proves which published number, the three tree-gate clips that get spliced, and the ~90 s doc-claim re-verification set
- [Dual-track direction (2026-08-25)](project_dual_track_direction.md) — portfolio v1 AND an extractable avoidance-core product seed; how that re-ranks findings and the drone-engineer lens
- [Red-team method](feedback_red_team_method.md) — the recurring failure patterns when this project grades itself: n=1 re-ranking, decision-layer-only gates, unrun remedies, severity inflation, unfollowed URLs, multi-builder prose drift, firmware identifiers reasoned instead of fetched at the pinned SHA, multi-builder rounds leaking at the HANDOFF rather than inside a file, grading hardest the numbers a round itself introduces, outward copy dropping the inconvenient qualifier, and docs/drafts being PUBLISHED not private
- [Portfolio-floor decision (2026-09-10)](project_portfolio_floor_decision.md) — ADR-022: portfolio first, then a ~10-conversation market test of the METHOD; wire program CUT, NDVI frozen; what that re-ranks for QA

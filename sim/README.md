# Gazebo simulation assets

- `worlds/` — the custom farm world (bounded field polygon, tree rows, bird actors)
- `models/` — drone model, NDVI camera sensor, tree/bird models

Owned by `robotics-sim-engineer`. Keep scenario parameters data-driven where possible so eval can add cases without code changes.
